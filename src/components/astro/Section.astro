---
import clsx from "clsx";

interface Props {
  htmlTag?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { htmlTag: Tag = "section", class: className } = Astro.props as Props;
---

<Tag class={clsx(`px-[4%]`, className)}>
  <slot />
</Tag>
